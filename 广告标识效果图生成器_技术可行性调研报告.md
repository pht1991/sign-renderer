# 广告标识外立面安装效果图生成器 — 技术可行性调研报告

> 调研日期：2026-07-25  
> 调研人：小游 🎮

---

## 一、需求拆解

应用核心功能链路：

| 环节 | 输入 | 处理 | 输出 |
|------|------|------|------|
| ① 上传照片 | 建筑外立面照片（JPG/PNG 位图） | 加载到画布 | 可交互底图 |
| ② 上传标识 | 广告标识矢量图（SVG/AI/EPS） | 解析为路径数据 | 可渲染矢量对象 |
| ③ 标记点位 | 用户在照片上点选上下左右4个点 | 计算透视区域 | 放置位置 + 透视参数 |
| ④ 设置参数 | 厚度、材质、颜色等 | 参数化建模 | 3D 几何体 |
| ⑤ 渲染合成 | 底图 + 3D标识 + 透视矩阵 | 透视变换 + 光照合成 | 安装效果图 |
| ⑥ 导出 | 最终画布 | canvas → 图片 | PNG/JPG 交付件 |

**本质问题**：将 2D 矢量图形，经过透视变换贴合到照片上，并赋予真实 3D 厚度与光照效果，生成以假乱真的安装效果图。

---

## 二、核心技术难点分析

### 难点 1：矢量图格式解析

| 格式 | 支持情况 | 方案 |
|------|---------|------|
| **SVG** | ✅ 原生支持 | Three.js `SVGLoader` 解析路径 → `createShapes()` 转 `THREE.Shape` |
| AI (Adobe Illustrator) | ⚠️ 需转换 | 前端不直接支持，需服务端用工具转 SVG，或引导用户导出 SVG |
| EPS | ⚠️ 需转换 | 同上，建议 MVP 阶段只接受 SVG |
| PDF 矢量 | ⚠️ 需转换 | `pdf.js` 可提取矢量路径，但复杂度较高 |

**结论**：MVP 阶段锁定 SVG 格式，覆盖 80%+ 场景。后续按需扩展 AI/EPS 服务端转换。

**已验证方案**（搜索结果确认）：
```
SVGLoader.parse(svgString) → paths[]
SVGLoader.createShapes(path) → THREE.Shape[]
new THREE.ExtrudeGeometry(shape, { depth: 厚度 }) → 3D 几何体
```

**已知坑**：SVG 坐标系 Y 轴向下，Three.js Y 轴向上，需 `group.scale.y *= -1` 翻转。

### 难点 2：透视变换（四点定位 → 贴合）

用户在建筑照片上标记 4 个点（上左、上右、下右、下左），定义广告标识要贴附的四边形区域。需要将矩形矢量图透视变换到该四边形。

**数学本质**：计算 3×3 单应性矩阵（Homography Matrix），将源矩形坐标映射到目标四边形坐标。

**成熟开源方案**（搜索验证）：

| 库 | 类型 | 特点 |
|----|------|------|
| `perspective-transform` | 纯 JS | 计算 homography 矩阵，无依赖，轻量 |
| `perspective.js` | Canvas 2D | 直接在 canvas 上绘制透视图像 |
| `glfx.js` / `webgl-distort` | WebGL | GPU 加速，支持全分辨率实时变换 |
| `dltjs` + Three.js Shader | WebGL | 在 Three.js 中用 fragment shader 做透视 warp |

**推荐方案**：在 Three.js 场景中用 homography 矩阵驱动 fragment shader 做透视变换。这样 3D 厚度效果和透视变换在同一个 WebGL 上下文中完成，无需切换渲染管线。

**难度**：⭐⭐⭐ 中等，数学成熟，有现成实现。

### 难点 3：3D 厚度效果 ⭐ 关键差异点

"厚度"是用户明确要求的参数，也是效果真实度的核心。

| 方案 | 实现方式 | 效果 | 性能 |
|------|---------|------|------|
| **A. 2D 模拟** | 阴影偏移 + 渐变 + 边缘描边 | 快但"假"，无真实侧面 | ⭐⭐⭐⭐⭐ 极快 |
| **B. 真 3D 拉伸** | `ExtrudeGeometry` 生成实体几何体 | 真实侧面、可打光、可投影 | ⭐⭐⭐ 中等 |
| **C. 真 3D + 倒角** | ExtrudeGeometry + bevel 圆角 | 最真实，接近实物 | ⭐⭐ 较重 |

**推荐方案 B**，理由：
- 厚度是核心参数，必须用真 3D 才能体现参数变化的效果差异
- `ExtrudeGeometry` 是 Three.js 原生功能，搜索验证了完整的 SVG→Shape→Extrude 链路
- 可配合方向光产生侧面阴影，真实度高
- 性能在合理范围内（单标识几何体顶点量可控）

### 难点 4：光照与环境融合

要让广告标识"看起来真的贴在建筑上"：

| 子问题 | 方案 | 难度 |
|--------|------|------|
| 光照方向匹配 | 用户手动拖拽光源方向 / 从照片自动分析亮暗 | 中 |
| 阴影投射 | Three.js `ShadowMap` 或模拟投影 | 中 |
| 材质表现 | `MeshStandardMaterial` / `MeshPhysicalMaterial`，支持金属、亚克力等 | 中 |
| 环境反射 | 从照片采样环境光色温，调整标识材质 | 高（可选） |

**建议**：MVP 阶段先做"手动光源 + 基础阴影"，后续迭代加入环境光自动分析。

### 难点 5：交互精度

| 交互需求 | 方案 |
|---------|------|
| 四点拖拽定位 | 鼠标/触摸事件 + 拖拽手柄 |
| 高分辨率照片缩放平移 | Canvas 变换 + 视口控制 |
| 微调精度 | 支持键盘方向键微调 + 放大镜辅助 |
| 网格/辅助线 | 透视网格参考线，帮助对齐 |

---

## 三、技术方案对比

### 方案 A：纯前端 Web 应用（React + Three.js）⭐ 推荐

```
┌─────────────────────────────────────────────┐
│              浏览器（零安装）                  │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐  │
│  │ React UI │  │ 交互层   │  │ Three.js  │  │
│  │ 上传/参数 │→│ 四点标记 │→│ WebGL渲染 │  │
│  └──────────┘  └──────────┘  └───────────┘  │
│                     ↓               ↓        │
│              Homography矩阵    ExtrudeGeometry│
│                     ↓               ↓        │
│              透视变换Shader    3D厚度+光照    │
│                     └───────┬───────┘        │
│                             ↓                │
│                      Canvas 合成 → 导出      │
└─────────────────────────────────────────────┘
```

**技术栈**：
- React 18 + TypeScript
- Three.js + @react-three/fiber + @react-three/drei
- SVGLoader（矢量解析）
- perspective-transform + 自定义 shader（透视变换）
- Zustand（状态管理）
- Ant Design / TailwindCSS（UI）

**优点**：跨平台、零安装、迭代快、生态成熟、搜索验证技术链路完整  
**缺点**：矢量格式限于 SVG；超大图片受浏览器内存限制（约 4K 纹理上限）  
**可行性**：⭐⭐⭐⭐⭐

### 方案 B：桌面应用（Electron + Three.js）

在方案 A 基础上用 Electron 封装，增加：
- 本地文件直接读写
- 集成 OpenCV/Cairo 做高级图像处理
- AI/EPS 格式通过本地工具链转换
- 不受浏览器内存限制

**优点**：格式支持广、性能好、专业感强  
**缺点**：开发成本 +30%，需打包分发  
**可行性**：⭐⭐⭐⭐

### 方案 C：移动端 App / 小程序

**优点**：拍照方便  
**缺点**：算力受限，Three.js 在小程序中支持有限，3D 效果实现难度大幅增加  
**可行性**：⭐⭐  
**结论**：不推荐作为首选，可作为 Web 版稳定后的拍照采集端。

---

## 四、推荐方案与理由

### 推荐：方案 A（Web 应用），预留方案 B 升级路径

| 决策因素 | 判断 |
|---------|------|
| 技术成熟度 | ✅ 两大核心技术（SVG→3D 拉伸、透视变换）均有成熟开源方案，搜索已验证 |
| 开发成本 | ✅ 最低，无需服务端复杂逻辑（纯前端渲染） |
| 上手门槛 | ✅ 零安装，用户打开网页即用 |
| 效果上限 | ✅ Three.js 真 3D + WebGL shader，效果质量有保障 |
| 扩展性 | ✅ 如后续需 AI/EPS 格式或更高性能，可平滑升级 Electron |

**一句话**：用最成熟的技术栈、最低的成本，最快验证产品逻辑；效果不够再升级，格式不够再加服务端。

---

## 五、风险评估

| 风险项 | 影响 | 概率 | 应对措施 |
|--------|------|------|---------|
| 复杂矢量图渲染异常 | 标识显示错误 | 中 | MVP 限定 SVG 格式，提供格式规范文档；多路径/渐变做兼容测试 |
| 效果真实度不达标 | 客户不满意 | 中 | 光照+材质专项调优（2-3 周）；提供材质预设库 |
| 高分辨率照片卡顿 | 交互体验差 | 中 | 图片压缩 + 分块加载；WebGL 纹理尺寸控制 |
| 四点定位精度不足 | 贴合不准 | 低 | 提供网格辅助线 + 键盘微调 + 放大镜功能 |
| 浏览器兼容性 | 部分用户无法使用 | 低 | 限定现代浏览器（Chrome/Edge/Safari 最新版），WebGL 2.0 |

---

## 六、技术选型清单

| 类别 | 选型 | 用途 |
|------|------|------|
| 前端框架 | React 18 + TypeScript | UI 框架 |
| 3D 引擎 | Three.js + @react-three/fiber | 3D 渲染 + 矢量拉伸 |
| 辅助库 | @react-three/drei | 相机控制、辅助工具 |
| 矢量解析 | three/addons/loaders/SVGLoader | SVG → THREE.Shape |
| 透视变换 | perspective-transform + 自定义 GLSL shader | 四点 homography 变换 |
| 状态管理 | Zustand | 参数状态管理 |
| UI 组件 | Ant Design 5 / TailwindCSS | 界面组件 |
| 图像导出 | canvas.toBlob() / renderToDataURL | 效果图导出 |
| 构建工具 | Vite | 开发与打包 |

---

## 七、时间估算

| 阶段 | 内容 | 周期 |
|------|------|------|
| **P0 原型验证** | SVG 加载 → ExtrudeGeometry 拉伸 → 四点透视变换 → 合成一张图 | 1-2 周 |
| **P1 核心功能** | 完整交互：上传、标记、参数面板、实时预览、导出 | 3-4 周 |
| **P2 效果调优** | 光照匹配、材质预设、阴影投射、边缘抗锯齿 | 2-3 周 |
| **P3 打磨上线** | 边界处理、错误提示、性能优化、部署 | 1-2 周 |
| **合计** | | **7-11 周**（出可用 MVP） |

> 💡 如果先做 P0 原型验证（1-2 周），可以快速判断"效果能不能过关"这个最大不确定性，再决定是否全力投入。

---

## 八、结论

**技术可行性：✅ 可行。** 两大核心技术支柱——SVG 矢量图 3D 拉伸（Three.js ExtrudeGeometry）和四点透视变换（Homography + WebGL Shader）——均有成熟的开源实现和充分的社区验证，不存在技术黑盒风险。

**最大不确定性**不在"能不能做"，而在"效果够不够真"——这取决于光照和材质的调优深度，建议用 P0 原型（1-2 周）先验证效果上限。

**建议下一步**：先做 P0 原型验证，拿几张真实建筑照片 + 真实广告标识 SVG 跑通完整链路，看效果是否达标，再决定投入节奏。
